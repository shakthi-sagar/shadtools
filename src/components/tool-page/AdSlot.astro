---
export interface Props {
  slotId?: string;
}

const { slotId = 'default' } = Astro.props;
---

<div class="p-4 rounded-xl bg-slate-900/30 border border-slate-800/60 text-center text-xs text-slate-500" data-ad-slot={slotId}>
  <span>Advertisement</span>
</div>
