---
import { Base64EncodeTool } from '@/tools/base64/encode/Base64EncodeTool';

interface Props {
  config?: any;
}

const { config } = Astro.props;
---

<Base64EncodeTool client:load config={config} />
