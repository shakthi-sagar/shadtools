---
import { CurrencyConverterTool } from '@/tools/currency/converter/CurrencyConverterTool';

interface Props {
  config?: any;
}

const { config } = Astro.props;
---

<CurrencyConverterTool client:load config={config} />
