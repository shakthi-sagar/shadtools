---
import { ImageCompressorTool } from '@/tools/images/compress/ImageCompressorTool';

interface Props {
  config?: any;
}

const { config } = Astro.props;
---

<ImageCompressorTool client:load config={config} />
