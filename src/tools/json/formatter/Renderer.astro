---
import { JsonFormatterTool } from '@/tools/json/formatter/JsonFormatterTool';

interface Props {
  config?: any;
}

const { config } = Astro.props;
---

<JsonFormatterTool client:load config={config} />
